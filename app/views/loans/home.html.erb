<div class="w-full">
  <h1 class="font-bold text-4xl">Loans#home_page</h1>
  <p>Find me in app/views/loans/home.html.erb</p>

  <h2 class="font-bold">Real Estate Profit and Return Calculator</h2>
  <%= render 'loans/notice' %>
  <!-- Render the real_estate partial and pass the @real_estate instance variable as a local variable -->
  <%= render partial: 'loans/real_estate', locals: { real_estate: @real_estate } %>
</div>
